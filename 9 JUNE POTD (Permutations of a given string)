# GFG-DAILY-PROBLEM


class Solution
{
  public:
  vector<string>find_permutation(string s)
        {
            vector<string>v;
            sort(s.begin(),s.end());
            // Code here there
            do{
               v.push_back(s);
            }while(next_permutation(s.begin(),s.end()));
            
            return v;}
};
  
  
  
  BY:ABHAY GUPTA
